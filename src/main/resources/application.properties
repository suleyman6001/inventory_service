spring.application.name=inventory_service
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# Port number can be changed in server.port property
server.port=8082
spring.main.banner-mode=off

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DB_URL:NOT_CONFIGURED}

spring.datasource.username=${DB_USER:NOT_CONFIGURED}
spring.datasource.password=${DB_PASS:NOT_CONFIGURED}

# Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false

# Kafka properties
spring.kafka.bootstrap-servers=localhost:9094

# Kafka topics
app.kafka.topics.order-created=order-created

# Kafka producer properties
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
# Kafka producer event type mappings

# Kafka consumer properties
spring.kafka.consumer.group-id=inventory-service
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*
# Kafka consumer event type mappings
spring.kafka.consumer.properties.spring.json.type.mapping=OrderCreatedEvent:com.suleyman6001.inventory_service.kafka.event.OrderCreatedEvent
